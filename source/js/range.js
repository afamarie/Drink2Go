const minToggle = document.querySelector('#price-min-toggle');
const maxToggle = document.querySelector('#price-max-toggle');
const rangeBar = document.querySelector('.product-filters__custom-range-bar');

const MIN = 0;
const MAX = 900;

const getMin = () => {

}

const getMax = () => {

}

minToggle.addEventListener('');
maxToggle.addEventListener();
